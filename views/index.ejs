<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Travel Journal</title>
    <link rel='stylesheet' href="/css/styles.css">
  </head>
  <body>
    <nav>
      <% if (user) { %>
          <a href="/">Home</a>
          <a href= ''>Passport Page</a>
          <a href="/auth/signout">Sign out</a>
      <% } %>
    </nav>

    <h1>Travel Journal</h1>
    <p>A place to store all of your travel notes and memories</p>
    <% if (user) { %>
      <h2>Welcome to the app, <%= user.username %>!</h2>
      <p>
        <a href="/users/<%= userId %>" class="button">Passport Page</a>
      </p>
      <% } else { %>
      <h2>Welcome to the app, guest.</h2>
      <p>
        <a href="/auth/signup">Sign up</a> <a href="/auth/signin">Sign in</a>
      </p>
    <% } %>

  </body>
</html>